# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Screenshots produced at gates
/screenshots/

# Working documents — the brief, the spec, the rulebook, the decision log and
# the phase prompts. These are the private inputs the site was built from, not
# part of what is published. Keep them locally; they are deliberately untracked.
CLAUDE.md
CONCEPT.md
SPEC.md
PROGRESS.md
DECISIONS.md
oryzo-*.md

# Local environment overrides (.env itself is committed — it holds only the
# public site URL and no secrets).
.env.local
.env.*.local
